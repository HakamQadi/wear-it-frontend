'use client';
import Link from 'next/link'; import { Check } from 'lucide-react'; import { useEffect,useState } from 'react'; import { Header } from '@/components/Header'; import { Footer } from '@/components/Footer';
export default function OrderSuccess(){const [order,setOrder]=useState<{number:string;total:number}|null>(null);useEffect(()=>{try{setOrder(JSON.parse(sessionStorage.getItem('wear_it_last_order')||'null'))}catch{}},[]);return <><Header/><main className="successPage"><div className="successCard"><div className="successIcon"><Check size={30}/></div><h1>Order received.</h1><p>{order?<>Your order <strong>{order.number}</strong> for <strong>${order.total.toFixed(2)}</strong> is now pending confirmation.</>:'Your order has been placed and is pending confirmation.'}</p><Link href="/shop" className="button">Keep shopping</Link></div></main><Footer/></>}
